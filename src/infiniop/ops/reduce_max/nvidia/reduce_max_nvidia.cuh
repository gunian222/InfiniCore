#ifndef __REDUCE_MAX_NVIDIA_H__
#define __REDUCE_MAX_NVIDIA_H__

#include "../reduce_max.h"   // 引入通用 reduce_max 算子头文件

DESCRIPTOR(nvidia)           // 用已有的宏展开成 NVIDIA 后端的描述符类

#endif // __REDUCE_MAX_NVIDIA_H__
